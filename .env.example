# Public challenge build. No credentials are required for the baseline.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Leave false unless a trusted reverse proxy is intentionally configured.
WEATHER_TRUST_PROXY_HEADERS=false
